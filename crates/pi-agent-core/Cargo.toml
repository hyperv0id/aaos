[package]
name = "pi-agent-core"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["sync", "rt", "macros", "time"] }
futures = "0.3"
async-trait = "0.1"
serde_json = "1"
parking_lot = "0.12"

[dev-dependencies]
tokio-test = "0.4"
