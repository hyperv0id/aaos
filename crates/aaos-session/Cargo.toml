[package]
name = "aaos-session"
version = "0.1.0"
edition = "2021"

[dependencies]
aaos-tools = { path = "../aaos-tools" }
pi-agent-core = { path = "../pi-agent-core" }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "sync"] }

[dev-dependencies]
serde_json = "1"
tempfile = "3"
